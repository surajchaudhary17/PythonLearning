import os, sys, traceback, shutil, typing, datetime, time, json, http, seaborn
import os.path, socket, subprocess, requests, re, regex, glob

dir1 = dir(os.path.dirname("hello"))
print(dir1)
for i in range(10):
    print("Welcome to the new world")
